apiVersion: v1
kind: Service
metadata:
  name: projet-bda-service
spec:
  type: NodePort
  selector:
    app: projet-bda
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
    nodePort: 30000
